<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <div class="well">
        <% if params[:plan] == '2' %>
          <h2 class="text-center">Pro Account Registration</h2>
          <p>Sign up for the pro account for £10/month and get access to the community and contact information!</p>
          <br/>
          <%= render "pro_form" %>
        <% else %>
          <h2 class="text-center">Basic Account Registration</h2>
          <%= render "basic_form" %>
        <% end %>
      </div>
    </div>
  </div>
</div>